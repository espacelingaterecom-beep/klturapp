{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix app/web\" \"npm run dev --prefix app/api\"",
    "build": "concurrently --raw \"npm run build --prefix app/web\"",
    "start": "concurrently --kill-others --raw \"npm run start --prefix app/api\"",
    "lint": "concurrently --raw \"npm run lint --prefix app/web\" \"npm run lint --prefix app/api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "workspaces": [
    "app/web",
    "app/api"
  ]
}